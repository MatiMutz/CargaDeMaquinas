@page "/Pdf/{codigo}"

<embed src="@(codigo.Substring(0,7)).pdf" type="application/pdf" width="100%" height="600px" />

@code {
    [Parameter]
    public string codigo { get; set; }

    protected override void OnInitialized()
    {
        codigo = codigo ?? "fantastic";
    }
}